<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in pages %}
        <url>
            <loc>{{ url_for(page['url'], _external=True) }}</loc>
            <lastmod>{{ page['lastmod'] }}</lastmod>
        </url>
    {% endfor %}
</urlset>